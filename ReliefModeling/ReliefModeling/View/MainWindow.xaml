<Window x:Class="ReliefModeling.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525"
        DataContextChanged="MainWindow_OnDataContextChanged">
    <Grid ShowGridLines="True">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height = "Auto"/>
        </Grid.RowDefinitions>

        <ToolBarTray Grid.Row="0">
            <ToolBar>
                <Button Content="Load Bitmap"
                        Command="{Binding CommandLoadImage}"/>
                <Button Content="Load Legend"
                        Command="{Binding CommandLoadLegend}"/>
                <Button Content="Convert To 3D"
                        Command="{Binding CommandConver2DTo3D}"/>
            </ToolBar>
        </ToolBarTray>
        
        <Grid ShowGridLines="True" 
            Grid.Row="1">
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid ShowGridLines="True"
                  Grid.Column="0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                
                <Image Grid.Column="0"
                       Source="{Binding BitmapImage}"/>
                <Image Grid.Column="1"
                       Source="{Binding BitmapLegend}"/>

            </Grid>
            
            <WindowsFormsHost Grid.Column="1"
                              x:Name="ControlView3DObject"/>
            
        </Grid>

        <StackPanel Grid.Row="2">

            <TextBox Text="{Binding Path=Logger.Log, UpdateSourceTrigger= PropertyChanged}" />
        </StackPanel>

    </Grid>
</Window>